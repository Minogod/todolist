server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todolist?serverTimezone=Asia/Seoul
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
service:
  url: http://localhost:8080/